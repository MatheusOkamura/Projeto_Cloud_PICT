# Variáveis de Ambiente - Backend
# Copie este arquivo para .env e configure suas variáveis

# =============================================================================
# Microsoft Azure AD OAuth 2.0 - AUTENTICAÇÃO COMPLETA
# =============================================================================
# Para habilitar autenticação OAuth 2.0 com Microsoft:
# 1. Acesse: https://portal.azure.com
# 2. Azure Active Directory > App registrations > New registration
# 3. Redirect URI: http://localhost:8000/api/auth/callback (Web)
# 4. Copie: Tenant ID, Client ID
# 5. Certificates & secrets > New client secret > Copie o VALUE
# 6. API permissions:
#    - Microsoft Graph (Delegated): User.Read, openid, profile, email
#    - Microsoft Graph (Application): User.Read.All (para validação)
# 7. Grant admin consent para todas as permissões
#
# IMPORTANTE: Se não configurado, o sistema funciona em modo desenvolvimento
# sem validação Microsoft (aceita qualquer email @ibmec.edu.br)
# =============================================================================
MICROSOFT_TENANT_ID=your-tenant-id-here
MICROSOFT_CLIENT_ID=your-client-id-here
MICROSOFT_CLIENT_SECRET=your-client-secret-here
MICROSOFT_REDIRECT_URI=http://localhost:8000/api/auth/callback
FRONTEND_URL=http://localhost:5173

# =============================================================================
# Database
# =============================================================================
# Desenvolvimento (SQLite)
DATABASE_URL=sqlite:///./iniciacao_cientifica.db

# Produção (PostgreSQL)
# DATABASE_URL=postgresql://user:password@localhost/ibmec_ic

# =============================================================================
# JWT / Authentication
# =============================================================================
JWT_SECRET_KEY=sua-chave-secreta-super-segura-aqui-mude-em-producao
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60

# =============================================================================
# Backend Configuration
# =============================================================================
BACKEND_URL=http://localhost:8000
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# =============================================================================
# Email (futura implementação)
# =============================================================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu-email@ibmec.edu.br
SMTP_PASSWORD=sua-senha

# =============================================================================
# AWS S3 (para upload de arquivos em produção)
# =============================================================================
AWS_ACCESS_KEY_ID=sua-access-key
AWS_SECRET_ACCESS_KEY=sua-secret-key
AWS_BUCKET_NAME=ibmec-ic-files
AWS_REGION=us-east-1
